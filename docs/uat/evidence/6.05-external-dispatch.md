# 6.05 evidence — external agent execution path, end to end on testnet

Run date: **2026-09-17**, 03:03Z–04:00Z. Network: **testnet**. Target:
`https://orizons.xyz` (backend reached through the frontend's `/api/*` proxy).
Every value below was produced by this run and is quoted from its own output.

> **Which build this describes.** The entry check failed (D-036): the deployed
> backend has the binding routes and a `dispatch_signer`, but not the 2.06
> settlement route, so it predates backend `main`. The run went ahead against
> that build by decision. Nothing here is a statement about `main`; where `main`
> differs, the defect says so.

## Who did what

| role | identity | notes |
| --- | --- | --- |
| Operator (owner wallet) | `GBWMD26IB6CMG3JO3HU7SD7ZJSTF4BIJ5JS77ANMLJ52M6FV6K3J7BQJ` | throwaway testnet key, friendbot tx `12b65f9f…a09cef` |
| Buyer (payer wallet) | `GDJHP2I6NRCWYZTB3ZOXRE74V4M4EGXRYORGNPTGQ6BVNJNSSJO4PKXJ` | throwaway testnet key, friendbot tx `db678e32…8f3b88` |
| Operator endpoint | `tools/operator-endpoint/server.ts` | run locally, published over a Cloudflare quick tunnel |
| Dispatch signer (published) | `GB5MKHDFLJZ6OFPAHM7R4HGBUPFV5PZYL3W27VTIUZZ25JMQSDZBKCMR` | `GET /api/stellar/network → dispatch_signer` |

Both wallets were signed from a script (Python `stellar_sdk`, `Keypair.sign`
on the XDR the API built, `Keypair.sign_message` for the bind challenge) rather
than through a browser wallet. The team owns this agent, so this run proves the
plumbing, not that an outside operator can do it unaided — that is story 6.06.

## 1 · Registration

```
POST /api/stellar/build/register-agent   200  request 0809546e949b49bc
  {"owner":"GBWM…7BQJ","agent_id":"uat605_ext_op","name":"UAT 6.05 haiku operator",
   "skills":["haiku","poetry","uat605"],"price_usdc":0.01}
POST /api/stellar/submit                 200  request 1dce921f1cee4f0e
  {"hash":"64ad14cd6516a93b8a2e9e2564bd5cc15c0d9ccfc74ede2694cd7fa47f11fa3e","status":"SUCCESS","ledger":4718149}
```

Registration tx: <https://stellar.expert/explorer/testnet/tx/64ad14cd6516a93b8a2e9e2564bd5cc15c0d9ccfc74ede2694cd7fa47f11fa3e>

Read back from the chain (`GET /api/stellar/agent/uat605_ext_op`, request
`6ae974dd2c094a73`): `active true`, owner `GBWM…7BQJ`, `price 100000`
(0.01 in 7-decimal units), skills `haiku, poetry, uat605`,
`registered_at 1789614332`. After `POST /api/stellar/agents/sync`
(`{"synced": 6}`) the marketplace lists it with `"source": "onchain"`.

## 2 · Binding

```
GET  /api/agents/bind/endpoint-check?url=https://couples-experimental-saved-job.trycloudflare.com/dispatch
     {"allowed":true,"rule":null,"message":null}
POST /api/agents/uat605_ext_op/bind/challenge   200  request 31761a50f3324f8d
     message "orizon-bind:v1:uat605_ext_op:https://couples-experimental-saved-job.trycloudflare.com/dispatch:5b24f4be891d7e05c159a4315b7e3912"
POST /api/agents/uat605_ext_op/bind             200  request c70ce02b6893431c
     {"endpoint_url":"https://couples-experimental-saved-job.trycloudflare.com/dispatch",
      "owner":"GBWM…7BQJ","bound_at":1789614555.0854018,"replaced":false}
GET  /api/agents/uat605_ext_op/binding          200  request f96d6fbc63dd4656
     {"endpoint_url":"https://couples-experimental-saved-job.trycloudflare.com", "owner":"GBWM…7BQJ", "bound_at":1789614555.0854018}
```

The binding reads back. Anonymously it returns the origin only, in a field
still named `endpoint_url` (see Friction). The binding was later replaced twice
for the failure cases — `https://scanme.nmap.org:444/dispatch` (request
`160efaa820e54ba8`, `replaced: true`) and
`https://profiles-confident-plot-acm.trycloudflare.com/dispatch` (request
`696127e188c04611`, `replaced: true`), which is the binding left in place.
