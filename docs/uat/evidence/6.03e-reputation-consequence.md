# 6.03e evidence — the reputation consequence and its effect on routing

Run 2026-09-25, on testnet. Every rating quoted here is a real testnet
transaction on the drill's own ReputationLedger. Nothing was signed with the
deployment's key, and nothing touched its ledger or its money.

## 1 · What could run on the deploy

No dispute can be upheld on the deploy. No run settles (D-050), and
adjudication answers `503 dispute_refunds_disabled` (D-051). Probed live on
2026-09-25:

| Probe | Answer | What it shows |
| --- | --- | --- |
| `GET https://orizon-agents-be-stellar.onrender.com/readiness` | `ratings.writer` `scorer`; signer = scorer = `GDB4N25U…CDHP` | the story's prerequisite holds: the deploy's signer may write ratings |
| `GET /api/stellar/reputation/agt_09l5` | `count` 9, `disputed` 0, `dispute_rate_bps` 0, `smoothed_bps` 7000, `onchain` | the agent the drill disputes has never been disputed on the deploy |
| `POST /api/orchestrator/decompose` "Build me a calculator app" | agt_09l5 stamped `rep_count` 9, `rep_dispute_rate_bps` 0, `rep_bps` 7000 | a plan stamps what the route reads |
| `POST /api/disputes/dsp_uat_probe/uphold` | `503 dispute_refunds_disabled` | nothing can be upheld (D-051) |

`tests/reputation-consequence.spec.ts` asserts the first three on every suite
run. RC-01 is pinned there as an expected failure until adjudication opens.

## 2 · The drill: a real backend on testnet

Backend origin/main `08efeda`, run as `uvicorn app.main:app --workers 1` on
PostgreSQL 17.6. `setup_testnet.py` created a new ReputationLedger from the
wasm the deployed ledger runs (`2fc4965a…439b`, read off the deployed
instance, not rebuilt), with the drill's settler `GA45ITAK…EGZ2` as admin and
scorer:

- ledger [`CAFZBHEK…5CRP`](https://stellar.expert/explorer/testnet/contract/CAFZBHEKKBPFR5RG33MJIMOLGXCP46E6ZGYBYISA3BCMVLY2H3RA5CRP),
  created in [`72c3ac25…b39b`](https://stellar.expert/explorer/testnet/tx/72c3ac2595852d70a908a5a31d23bbd0aea196040b73752cb73b9dd4670bb39b)
- credits paid in the drill's own test asset `UATUSD` (SAC `CCW66O22…UENL`)
- `/readiness` on the drill backend: `writer` `scorer`

Each run seeds two settled workflows. The disputed step 0 is agt_09l5's, quoted
at 0.10 USDC, and each workflow settles 0.35 USDC. Each step gets the settler's
own rating (90, kind `auto`), as settlement writes it. The buyer opens a
dispute on each step. One is upheld through `scripts/uphold_dispute.py` and
the other through `POST /api/disputes/{id}/uphold`. Two runs were made on the
same ledger: one at the production read TTL of 15 s, then one at 120 s.

## 3 · RC-01 — the numbers before and after

`GET /api/stellar/reputation/agt_09l5` on the drill backend, before either
dispute was opened and after both were upheld:

| Run | | `count` | `disputed` | `dispute_rate_bps` | `smoothed_bps` | `avg_bps` | `weight` (stroops) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TTL 15 s | before | 2 | 0 | 0 | 7032 | 9000 | 2 000 000 |
| | after | **4** | **2** | **5000** | **6935** | 5000 | 4 000 000 |
| TTL 120 s | before | 6 | 2 | 3333 | 6968 | 6333 | 6 000 000 |
| | after | **8** | **4** | **5000** | **6875** | 5000 | 8 000 000 |

For each uphold, `count` rose by one and so did `disputed`: two upheld
disputes added two each. `dispute_rate_bps` rose and the smoothed score fell.
The uphold script printed its own before and after for its one dispute:
"before this run: 3333 bps (2 of 6), after this run: 4285 bps (3 of 7), moved
+952 bps".

The settler's own rating was not withdrawn. The average moved to the weighted
mean of all ratings: (9000 × 2 + 1000 × 2) / 4 = 5000. It did not become the
dispute's 10/100. With the settler at 90 and the dispute at 10, each pair
averages 50, inside the 15–52.5 the story expects.
