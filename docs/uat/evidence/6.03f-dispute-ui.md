# 6.03f evidence — the dispute UI and receipt, in every state

Run 2026-09-25/26 on testnet. Every refund and every rating a receipt links to
is a real testnet transaction on the 6.03e drill's own ReputationLedger, paid in
its own test asset. Nothing was signed with the deployment's key.

## 1 · What could run on the deploy

None of the five states can be reached on orizons.xyz. No run settles, so there
is nothing to dispute (D-050). Adjudication answers
`503 dispute_refunds_disabled` (D-051), and refunds must stay off until the
private money-path defect D-053 is fixed. So there is no open, credited or
rejected dispute on the deploy to read, and Dan cannot yet uphold or reject one
for a live walk-through. Checked 2026-09-25: `GET /api/tasks/tsk_7fc5bc5ea95f15fc/disputes`
still answers `settlement: null`, and nothing in the frontend (`5105a8b`) or
the backend (`08efeda`) has changed since 6.03e.

The receipt was therefore read on the real trace page. The frontend `5105a8b`
ran under `next dev`, the backend `08efeda` ran on PostgreSQL 17.6, and the
disputes were real, with real signatures and real testnet transactions. The
drill is `tools/dispute-ui-drill/`. The steps that need a real phone, a real
screen reader, or Dan's hand on the deploy are in
`checklists/6.03f-phone-and-screen-reader.md`.

## 2 · DS-01 to DS-03 — every state, read by the payer

Final run 2026-09-26 00:02–00:14 (GMT+8): `browser.spec.ts`, 13 of 13, at the
desktop width and on the phone project (360px, touch). All transactions below
were checked `successful` on testnet Horizon. The payer is
`GBXFOAMY…5XTD`, and each disputed step is agt_09l5's "Researcher" step,
charged 0.1.

| State | What the payer reads | On-chain record | Check |
| --- | --- | --- | --- |
| open | "Under review"; "Raised" with the local time and age; "The platform is reviewing this dispute; if it is upheld, the step's credit is paid to your wallet and Researcher's reputation records the dispute."; "Up to 0.1 USDC would be credited to your wallet if upheld — funded by the platform, not clawed back from the agent."; the payer's own reason | none, and no links | FS-01 **pass** |
| crediting | "Refund in progress"; "The refund was submitted and is waiting for confirmation on Stellar; if it cannot be confirmed, the platform reconciles it by hand"; "Up to 0.1 USDC to be credited"; refund row "Submitted, waiting for confirmation" with the full hash; nothing reads "Refunded" or "Confirmed" | refund [`0f27b46e…768a`](https://stellar.expert/explorer/testnet/tx/0f27b46e380d7915f1b81b39a5db2f66d581b9b705cff3d0ed1aa797848a768a) | FS-02 **pass** |
| credited | "✓ Refunded"; "Done: you received 0.1 USDC, and it cost Researcher a dispute rating on its reputation."; on one line: "0.1 USDC credited to your wallet — funded by the platform, not clawed back from the agent."; both rows "✓ Confirmed on Stellar" with full hashes and links | refund [`cadcac8e…ba96`](https://stellar.expert/explorer/testnet/tx/cadcac8ec45f916d9ab53329521b3ba9122b84d6ca4a11ac7ccb774afc68ba96), rating [`2a2937cd…6fb4`](https://stellar.expert/explorer/testnet/tx/2a2937cda140383b82b871fd36dfb8db2415cb4f726592b47aa2444eeaaf6fb4) | FS-03 **pass** |
| credited, rating unconfirmed | "Done: you received 0.1 USDC; the dispute rating it costs Researcher is not confirmed yet."; rating row "what it will cost the agent" and "Submitted, waiting for confirmation" with its hash and link, and no ✓ | rating [`eb1e600d…65a1`](https://stellar.expert/explorer/testnet/tx/eb1e600d7c13de98c2a595dfbc19554a7bca6f73d3a79ae65d1b0a98ef6165a1) | FS-04 **pass** |
| rejected | "✕ Rejected"; "The platform did not uphold this dispute: no credit was issued, Researcher's reputation is unchanged, and the reason is below."; "Why it was rejected" and the adjudicator's words; no credit line and no links | none | FS-05 **pass** |
| upheld, no transfer | "✓ Upheld"; "… the transfer to your wallet is queued …"; refund row "No transaction on record" | none | FS-13 **fail**, D-070 |

**DS-02, both links.** On the credited receipt, the refund link and the rating
link are exactly `https://stellar.expert/explorer/testnet/tx/<hash>` for that
dispute's `refund_tx` and `rating_tx`. FS-03 then reads both transactions back
from Horizon, the record Stellar Expert shows:
- the refund is a `transfer` whose only `account_credited` effect is
  0.1000000 to the payer;
- the rating is a `submit` naming `agt_09l5` and `dispute`, and its job id
  starts with this dispute's sealed job's first 8 bytes.

**DS-03.** The amount and "funded by the platform, not clawed back from the
agent" are one `<p>` on every state that has a credit line.

Read in the drill, not the product: the settlement's charge and seal hashes are
seeded, because no charge can land (D-050), so those two links go nowhere. The
credit reads "USDC" while the drill pays its own UATUSD.
