# 6.03b evidence — attacking idempotency on the money path

Run 2026-09-24. Testnet only; nothing was signed or submitted.

> This is the story's own card. An earlier pass under the same number worked
> from the title alone and covered the refusal paths
> (`6.03b-dispute-refusals.md`); its DR criteria now sit under IB in
> `test-plan.md`.

## 1 · Could the attacks run on the deploy?

No. Checked at 13:49 UTC against `https://orizon-agents-be-stellar.onrender.com`:

- `POST /api/disputes/{id}/uphold` answers `503 dispute_refunds_disabled`
  (D-051), so no uphold — and therefore no transfer to count — is possible.
- `DP-01` still fails as expected: no settlement exists, so no step can be
  disputed (D-050, behind D-039).

Every attack in the card needs a settled dispute, and five of the seven need an
uphold. So none was fired at the service, and the buyer's account has no
settler credits to count. Rather than wait, each attack was run against the code
that will guard the money once those blockers clear: backend origin/main
`3347090` and frontend origin/main `e56a07a`, with the signer and the wallet
stubbed and nothing sent to any network.
