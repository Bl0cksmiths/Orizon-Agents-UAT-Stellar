# 6.06 checklist — wallets and a real phone

The parts of story 6.06 that automation cannot reach: a real wallet extension
opening a real prompt, a person pressing **Reject**, and a real phone. Everything
else in 6.06 is checked by `tests/operator-surfaces.spec.ts`.

Work through it in order and fill in every **Record** field as you go. Paste the
results table at the end back when you are done. Copy expected text *exactly*
as quoted: a difference is a finding, even a small one.

## Rules

- **Testnet only.** Every wallet on Testnet before you open orizons.xyz; fund
  each new account with friendbot, never with real funds.
- **Do not clean up.** Leave every registration and binding in place; they are
  evidence.
- **One new agent per wallet.** Use `uat606_<wallet>_1` (for example
  `uat606_freighter_1`), and `_2` if you need another. Never reuse an id from an
  earlier run.
- **Do not work around a failure.** If a wallet cannot do a step, record what it
  showed and move to the next wallet. A wallet that cannot sign a message is a
  finding, not a blocker.
- **Record what you see, not what you expected.** Screenshots are named
  `6.06-<wallet>-<step>.png`.

## Before you start

| # | Do | Record |
| --- | --- | --- |
| P1 | Install the wallets you have: Freighter, xBull, Albedo (web, no install), LOBSTR (extension), Hana, Rabet. Note the version of each. | wallet → version |
| P2 | In each wallet create a **new** account, switch the network to **Testnet**. | G-address per wallet |
| P3 | Fund each: open `https://friendbot.stellar.org/?addr=<G-address>` in the browser. Expect JSON containing `"successful": true`. | pass/fail per wallet |
| P4 | Pick an endpoint URL to bind. Any public `https://` URL you control works for 6.06, because dispatch does not have to succeed here. A quick tunnel (`cloudflared tunnel --url http://127.0.0.1:8787`) gives one, but **its URL changes every time it restarts**. | URL used |
| P5 | Decide **exactly** the URL string you will bind and write it down with its path. The reference agent README preflights `https://YOUR-AGENT.onrender.com/` (root, trailing slash) while its `.env.example` expects `…/dispatch`. Bind the same string every time in this checklist, character for character. | exact URL string |
| P6 | Pick a second URL for the rebind step (OS-05): same host is fine, different path, e.g. `…/dispatch-v2`. | second URL |

## A · Register, per wallet (desktop browser)

Repeat A, B and C for each wallet in turn. Clear site data for `orizons.xyz`
between wallets so the previous connection is not restored.

| # | Where | Do | Expect (exact) | Record |
| --- | --- | --- | --- | --- |
| A1 | `https://orizons.xyz/app/register` | Before touching anything, read the box directly above the submit button. | Kicker `▸ two signatures, one at a time`, then: "Listing an agent takes two signatures. This one is the on-chain registration transaction. Binding an endpoint afterwards takes a second, separate signature — a signed message that proves you own the agent. It moves no funds and costs no fee." | present before any click? · screenshot `6.06-<wallet>-A1.png` |
| A2 | same | Press **Connect Wallet**, choose this wallet, approve the connection. | Header shows the wallet name, a `testnet` badge and `◆ GXXX…XXXX`. | badge text · pass/fail |
| A3 | same | Fill **agent id** `uat606_<wallet>_1`, **display name** `UAT 6.06 <wallet>`, **skills** `uat606`, **price per step (USDC)** `0.01`. Tab out of agent id. | The id check reports the id available; **Register agent ▸** becomes enabled. | pass/fail |
| A4 | same | Press **Register agent ▸**, and **reject** in the wallet prompt. (This is OS-04, prompt 1.) | Neutral grey text, not a red box: "Signing cancelled — your details are saved. Click Register when you're ready." All four fields still hold what you typed. | exact message · fields kept? · screenshot `6.06-<wallet>-A4.png` |
| A5 | same | Press **Register agent ▸** again and **approve**. | An inline card: kicker `▸ next step`, "`uat606_<wallet>_1` is registered on-chain by G… Binding an execution endpoint takes one message signature — the id is carried into that step, so there is no form to fill in again." Buttons **Bind an endpoint ▸**, **View in marketplace ▸**, **⧉ copy evidence**. | tx hash (use **copy evidence**) · screenshot `6.06-<wallet>-A5.png` |

If A4 or A5 shows anything else — a red box, cleared fields, "Transaction failed", a
wallet error — record the exact text and continue to B only if registration
succeeded.

## B · Bind, per wallet — the second signature

| # | Where | Do | Expect (exact) | Record |
| --- | --- | --- | --- | --- |
| B1 | the A5 card | Press **Bind an endpoint ▸**. | Lands on `/app/bind?agent=uat606_<wallet>_1` with **agent id** already filled, and the current-binding panel reads "no endpoint bound yet — this will be the agent's first." | URL landed on · id pre-filled? · anything you had to re-enter |
| B2 | `/app/bind` | Type your exact URL from P5 into **endpoint url**, wait a second. | Hint `✓ endpoint allowed`. Button reads **Bind endpoint ▸**. | hint text |
| B3 | same | Press **Bind endpoint ▸**, and **reject** in the wallet prompt. (OS-04, prompt 2.) | Neutral grey text, not a red box: "Signing cancelled — nothing was bound. Your details are still here; press Bind endpoint when you're ready." Both fields keep their values. | exact message · fields kept? · screenshot `6.06-<wallet>-B3.png` |
| B4 | same | Press **Bind endpoint ▸** again and **approve**. While waiting, read the status line. | Status passes through "Requesting a signing challenge from the registry…", "Waiting for your wallet — open the wallet popup and approve the signature request to authorize this endpoint. · challenge valid for …", "Signature received. Binding the endpoint…". Then kicker `✓ endpoint bound` and "uat606_<wallet>_1 now receives work at <your URL> — its first binding." | what the wallet prompt itself said (does it show the message text?) · screenshot `6.06-<wallet>-B4.png` |

### Albedo and Rabet — expected to fail at B3/B4

The wallet kit the app ships (`@creit.tech/stellar-wallets-kit` 2.1.0) does not
implement message signing for these two, yet both are offered in the picker.
Expected on pressing **Bind endpoint ▸**, with **no wallet prompt opening at all**:

> `Transaction failed — Albedo does not support the "signMessage" function`
> (`Rabet does not support …` for Rabet), in a red alert box.

Confirm it and record the exact text and a screenshot. Do not try another wallet
for the same agent: registration by this wallet stands, and "registers fine,
cannot bind" is exactly the finding the story asks for. Mark B as **cannot sign
messages** in the results table.

If the wallet opens a prompt instead, or shows different text, record that — it
means the expectation above is wrong.

## C · Dashboard and rebind, per wallet

| # | Where | Do | Expect (exact) | Record |
| --- | --- | --- | --- | --- |
| C1 | the B4 success card | Press **View in my agents ▸**. | `/app/operator`, h1 `My Agents`, the agent card for `uat606_<wallet>_1`, tile `endpoint bound` reads `1` of `1`, and **Routing standing** shows "✓ An endpoint is bound, so this agent reaches the planner as a candidate." | where the hand-off lost you, if anywhere |
| C2 | `/app/operator` | Read the **Settlement** panel for this agent. | Either "⚑ No payment has settled to uat606_<wallet>_1." with the **Why nothing settles** note, **or** "Settlement could not be loaded for uat606_<wallet>_1. This is a failed lookup, not a zero…" (the deployed backend lacks the settlement route). No earnings is **expected** — do not report it as a bug; report only wording that claims more than is known. | which of the two · screenshot `6.06-<wallet>-C2.png` |
| C3 | `/app/bind?agent=uat606_<wallet>_1` | Wait for the current-binding panel to load. | It lists `endpoint` (host of your P5 URL), `owner` (this wallet), `bound`. The field label reads **replacement endpoint url**, the note "Binding replaces the endpoint above. Work already routed keeps the endpoint it was routed to; everything after this lands on the new one." and the button **Replace endpoint ▸**. | pass/fail |
| C4 | same | Enter the second URL from P6, press **Replace endpoint ▸**, approve. (OS-05.) | Kicker `✓ endpoint replaced`, "uat606_<wallet>_1 now receives work at <second URL> — this superseded the endpoint it was pointing at before." and the current-binding panel updates. | new `bound` time · screenshot `6.06-<wallet>-C4.png` |
| C5 | a terminal | `curl -s https://orizons.xyz/api/agents/uat606_<wallet>_1/binding` | `replaced: true` and the new `bound_at`. Anonymous reads return the host only, not the path. | paste output |

Rebind needs doing on **one** message-signing wallet only; skip C3–C5 for the
others once it has passed.
